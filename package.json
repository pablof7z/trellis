{
  "name": "trellis-site",
  "version": "0.1.0",
  "private": true,
  "description": "Static public website for Trellis.",
  "scripts": {
    "check": "node scripts/check-site.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
