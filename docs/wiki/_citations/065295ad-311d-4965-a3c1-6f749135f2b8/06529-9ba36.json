{
  "id": "06529-9ba36",
  "ts": "2026-07-04T12:47:31Z",
  "session": "065295ad-311d-4965-a3c1-6f749135f2b8",
  "text": "**The proof artifact:** a written case study — *\"tenex-edge: moving the authority frontier under a live agent fabric\"* — whose spine is the `probe seams` table time-lapse (Phase 0 baseline → each promotion), backed by two hard experiments and one beachhead.\n\n**Experiment A — the diagnosis metric (Story 3).** Seed a lifecycle bug (revert the refcount so a shared channel closes on first-owner departure, or disable status dedup). Run the incident twice: give a model (a) the serialized commit/trace stream + labels, (b) the raw `tracing` logs. Measure root-cause accuracy. Target ≥90% from receipts vs the log baseline; the delta is the publishable result and the literal demonstration of \"diagnosis becomes lookup, not abduction.\"\n\n**Experiment B — the safety invariant (Story 1).** Show, via the bypass ratchet + `probe simulate`, that on authoritative surfaces every effect was preceded by a previewed plan — \"no effect without a machine-reviewable plan first,\" enforced structurally.\n\n**Beachhead evidence tenex-edge already owns (Story 2 — lead with this).** The refcounted, close-on-last-owner subscription reconciler *already fixed a real unbounded leak* the old aggregate `SubscriptionRegistry` could not tear down (`subscriptions/mod.rs:1-25`, adoption doc §surfaces). `probe stats` quantifies it live: opens/closes/coalesced/`live_now`, \"N closes emitted, 0 leaks.\" This is the \"you have leaked subscriptions right now\" story, told by a shipping consumer with numbers — the honest version NMP's shadow-mode adoption can't yet tell because it isn't authoritative."
}
